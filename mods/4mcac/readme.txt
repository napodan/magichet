This mod adds new achievements to unlock that were inspired by MC ones.
Also, the following fork of Rubenwardy's "awards" mod is required:
https://github.com/4aiman/awards

(That fork adds used here on_globalstep trigger as well as enables achievements
to be shown as HUD, therefore not blocking user's input.)
